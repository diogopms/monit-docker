---
services:
  monit:
    image: ghcr.io/diogopms/monit-docker:latest
    container_name: monit
    environment:
      - TZ=Europe/Lisbon
    volumes:
      - ./monitrc:/etc/monitrc
    ports:
      - 2812:2812
    restart: unless-stopped
